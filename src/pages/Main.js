import React from 'react';

import Calendar from '../components/Calendar';

const Main = () => {
	// * 여기서 dispatch
	return (
		<React.Fragment>
			<Calendar />
		</React.Fragment>
	);
};

export default Main;
